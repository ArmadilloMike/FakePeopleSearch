when making on your own add your own .env file, Change the search api to brave search and the ai api url if not using hc ai.

This project generates a fake person using Faker and uses search api and ai api to make a summery about that person
